# 智展研究院设计开发管理系统 — 产品需求文档（PRD）

> **版本**：3.0 | **状态**：与代码同步 | **更新**：2026-08-10
>
> 定义全部功能模块、用户交互流程、边界情况处理。面向产品/前端/测试。

---

## 1. 产品概述

智展研究院设计开发管理系统是以流程驱动项目的业务管理平台。

**核心用户旅程**：
```
设计模板 → 发布 → 发起实例 → 节点执行 → 校验 → 审批 → [批准] → 下一节点 → ... → 终审 → 归档
```

**关键数字**：23 个前端路由，9 个个人中心 Tab，9 种实时通知，4 级难度，4 级优先级，2 种流程类型。

### 1.1 登录页 `/login`

```
┌──────────────────────────────────────┐
│                                      │
│        [Logo] 智展研究院设计开发管理系统         │
│        Enterprise Workflow Approval   │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 用户名                         │  │
│  │ [___________________________]  │  │
│  │                                │  │
│  │ 密码                           │  │
│  │ [___________________________]  │  │
│  │                                │  │
│  │ ☑ 记住用户名                   │  │
│  │                                │  │
│  │ [        登 录        ]        │  │
│  └────────────────────────────────┘  │
│                                      │
│  ⚠ 登录失败，请检查用户名和密码      │  ← 仅失败时显示
│                                      │
└──────────────────────────────────────┘
```

**交互细节**：
- 用户名字段：必填，前端校验非空
- 密码字段：必填，支持 show-password 切换明文/密文
- 回车键触发登录
- "记住用户名"：勾选后用户名存入 `localStorage`，下次打开自动填充
- 登录中：按钮显示 loading + "登录中…"，禁止重复点击
- 登录失败：红色 alert 显示错误原因（`err.message` 或默认"登录失败，请检查用户名和密码"）
- 登录成功：存储 Token → 跳转 `?redirect` 参数指定页面（无则默认 `/dashboard`）

**Token 过期处理**：
- Axios 响应拦截器捕获 401 → 清除 `localStorage` 中的 token → `window.location.href = '/login'`
- 登录页不检查登录态，已登录用户访问登录页会跳转到首页

### 1.2 面包屑导航

通过 `useBreadcrumb` composable 全局管理，每个页面在 `onMounted` 时设置。

| 页面 | 面包屑路径 |
|------|-----------|
| Dashboard | 首页 |
| 组织卡片 | 开工项目 |
| 所内主页 | 开工项目 > XX所 |
| 全部项目 | 开工项目 > 全部项目 |
| 模板详情 | 开工项目 > XX所 > 模板名称 |
| 流程设计器 | 开工项目 > XX所 > 模板名称 > 流程设计 |
| 实例详情 | 开工项目 > XX所 > 项目名称 |
| 方案列表 | 方案管理 |
| 所内方案 | 方案管理 > XX所 |
| 方案详情 | 方案管理 > XX所 > 方案名称 |
| 个人中心 | 个人中心 |
| 任务处理 | 个人中心 > 任务处理 |
| 校验处理 | 个人中心 > 校验处理 |
| 审批处理 | 个人中心 > 审批处理 |
| 批准处理 | 个人中心 > 批准处理 |
| 超期预警 | 超期预警 |
| 系统管理 | 系统管理 |

**行为规则**：
- 最后一级为当前页面，不可点击
- 中间级别为链接，点击跳转
- Dashboard 和设计器页面**不显示**面包屑栏
- 面包屑中通知铃铛仅在有面包屑的页面出现

---

## 2. 用户角色与菜单

| 角色 | 标识 | 可见菜单 | 可见个人中心 |
|------|------|----------|:--:|
| 系统管理员 | system_admin | 首页、开工项目、方案管理、系统管理 | 否 |
| 所长 | manager | 首页、开工项目、方案管理、个人中心 | 是（9 Tab） |
| 普通用户 | user | 首页、开工项目、方案管理、个人中心 | 是（6 Tab，无"我发起的"） |

> 系统管理员不参与业务，无个人中心。右上角下拉菜单：个人信息 + 签名上传 + 修改密码（所有角色共有）。

---

## 3. Dashboard（首页看板）

### 3.1 页面布局

```
┌─────────────────────────────────────────────────────┐
│  [项目 Tab] [方案 Tab]              [刷新] [🔔铃铛]   │
├─────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 进行中   │ │ 已归档   │ │ 本月归档 │ │ 预警/总数 │ │
│  │   12     │ │   45     │ │    8     │ │   3/25   │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
├─────────────────────────────────────────────────────┤
│  我的待办快捷条：待处理 N · 待校验 N · 待审批 N         │
├──────────────────────┬──────────────────────────────┤
│  各所项目分布饼图     │  卡点追踪表格                  │
│  (点击扇区→组织主页)  │  (优先级排序，逾期红色高亮)    │
├──────────────────────┴──────────────────────────────┤
│  各所统计柱状图（项目总数/运行中/已完成/已终止）       │
│  (点击柱子 → 组织主页)                               │
└─────────────────────────────────────────────────────┘
```

### 3.2 Tab 切换行为

- 切换 Tab 时，四个统计卡片、卡点追踪表格**全部切换**数据来源（项目/方案）
- 饼图和柱状图只展示项目数据（方案无饼图）
- 方案 Tab 的第四张卡片显示"方案总数"而非"超期预警/总数"

### 3.3 统计卡片

| 卡片 | 项目数据源 | 方案数据源 |
|------|-----------|-----------|
| 进行中 | status=running 的项目数 | status=running 的方案数 |
| 已归档 | 已完成的项目数 | 已归档方案数 |
| 本月归档 | 本月 completed_at 的项目数 | 本月完成的方案数 |
| 预警/总数 | 逾期+即将逾期任务数 / 总任务数 | 方案总数 |

### 3.4 我的待办快捷条

- 仅所长和普通用户可见（系统管理员不显示）
- 分别显示：待处理任务数、待校验数、待审批数
- 点击跳转个人中心对应 Tab

### 3.5 卡点追踪表格

列：项目名称、组织、难度（彩色 badge）、当前节点、负责人、**进度条**、逾期状态
- 按优先级排序（urgent > high > normal > low）
- 逾期行红色高亮
- 支持按组织下拉筛选
- 显示所有 running 状态实例

### 3.6 饼图

- 各组织运行中项目数量分布
- 点击扇区 → 跳转 `/flows/organization/{orgId}`

### 3.7 柱状图

- 每个组织四根柱子：总数 / 运行中 / 已完成 / 已终止
- 点击柱子 → 跳转对应组织主页

### 3.8 通知铃铛

- Dashboard 页面直接在 header 中渲染（不复用面包屑栏的位置）
- 有未读时显示红点 + 计数（超过 99 显示"99+"）
- 点击打开下拉面板，展示最近 20 条通知
- 未读项蓝色背景 + 蓝点标记
- 支持一键全部已读
- WebSocket 实时推送新通知时弹出气泡（5 秒自动消失）

### 3.9 边界情况

- **无数据**：饼图和柱状图显示空状态提示
- **加载中**：所有卡片和图表显示骨架屏/loading
- **WebSocket 断开**：静默重连，不弹错误提示

---

## 4. 流程管理（项目）

### 4.1 组织卡片页 `/flows`

- 以卡片网格展示所有启用组织
- 每张卡片显示：组织名称、运行中项目数、最近更新时间
- 当前用户所属组织**高亮边框**
- 点击卡片进入 `/flows/organization/{orgId}`
- 右上角"全部项目"按钮 → 全部项目列表页

### 4.2 全部项目 `/flows`（列表模式）

**状态筛选按钮栏**：
```
[全部 N] [运行中 N] [已完成 N] [已终止 N]
```

**高级搜索面板**（可折叠）：
- 日期范围（发起时间从…到…）
- 优先级下拉（urgent/high/normal/low）
- 发起人搜索
- 组织筛选

**表格列**：

| 列 | 说明 |
|----|------|
| 项目名称 | 可点击 → 实例详情 |
| 组织 | 所属所名称 |
| 优先级 | 彩色标签（红/橙/蓝/灰） |
| 难度 | 数字 badge（1/2/3/4），难度4 特别标记 |
| 进度 | 进度条（finished 节点数/总节点数） |
| 当前节点 | 当前活跃节点名称 |
| 状态 | 标签（运行中/已完成/已终止） |
| 发起人 | 姓名 |
| 发起时间 | 日期时间 |

**分页**：支持 page_size 选择器（20/50/100）

### 4.3 所内主页 `/flows/organization/{orgId}`

两个 Tab：

**流程实例 Tab**：
- 该组织下的实例列表（同全部项目表格结构）
- 所长可见"发起流程"按钮（蓝色醒目）
- 发起按钮 → 弹出创建实例对话框

**流程模板 Tab**：
- 该组织下的模板列表
- 列：模板名称、描述、节点数、状态（草稿/已发布/已停用）
- 所长可见：创建、编辑、删除、设计器入口
- 点击模板名称 → 模板详情
- 点击"设计" → 流程设计器

### 4.4 流程设计器 `/flows/designer/{templateId}`

仅所长可访问。LogicFlow 画布 + 属性面板。

**画布操作**：
- 左侧节点列表拖出到画布（预设节点 + 自定义节点）
- 节点拖拽移动
- 节点间连线（从节点右侧拖出到目标节点左侧）
- 选中节点 → 右侧属性面板
- 选中连线 → 可删除
- Ctrl+Z / Ctrl+Y 撤销重做
- 滚轮缩放、拖拽平移

**属性面板**（选中中间节点时）：
```
┌─────────────────────────┐
│ 节点名称          [___] │
│ 描述              [___] │
│ 负责人          [选择器] │
│ 校验人          [多选器] │
│ 审批人          [多选器] │
│ 批准人(可选)    [选择器] │  ← 仅 difficulty=4 时生效
│ 完成时限      [N] 工作日 │
│ ☑ 必须上传文件          │
│ 文件提交文件夹配置      │
│   ┌───────────────────┐ │
│   │ 文件夹名 必须 数量 │ │
│   │ [___]   [☑] [N]  │ │
│   │ [+ 添加]         │ │
│   └───────────────────┘ │
│ 签批配置                │
│ ☑ 负责人提交时签名      │
│ ☑ 校验人通过时签名      │
│ ☑ 审批人通过时签名      │
│ ☑ 批准人通过时签名      │
│ 签名 X/Y/页码: [ ] [ ] [ ]│
└─────────────────────────┘
```

**节点预设**：
- 保存当前节点配置为预设 → 左侧列表出现
- 拖出预设节点 → 自动填充配置
- 管理预设（编辑/删除）→ 弹窗

**节点列表视图**（画布下方）：
- 表格展示所有节点：序号、名称、负责人、校验人数、审批人数、时限
- 方便快速核对配置

**发布校验**：
- 至少 3 节点（开始 + ≥1 工作 + 结束）
- 中间节点：名称/负责人/校验人/审批人必填
- 所有节点 BFS 连通
- 不通过 → 红色提示具体原因

**文件模板关联**：
- 设计器中可关联/取消关联文件模板（多选）
- 关联后发起实例时可调整

### 4.5 发起流程对话框

```
┌────────────────────────────────┐
│ 发起流程                       │
│                                │
│ 选择模板    [下拉：仅已发布模板] │
│ 实例名称    [_______________]   │
│ 优先级      [下拉：urgent/high/normal/low] (默认 normal) │
│ 难度等级    [下拉：1/2/3/4] (默认 1)  │
│ 合同号      [_______________] (可选) │
│ 产品型号    [_______________] (可选) │
│ 销售经理    [_______________] (可选) │
│ 关联方案    [下拉：已完成的方案] (可选，仅项目) │
│                                │
│ 节点配置调整 (可折叠)           │
│  ┌─ 节点1: 负责人[可选] 时限[N] │
│  │  校验人[可选] 审批人[可选]    │
│  │  批准人[可选] 截止日期[可选]  │
│  ├─ 节点2: ...                │
│  └─ ...                       │
│                                │
│ 文件模板配置 (可折叠)           │
│  ┌─ ☑ 模板A  ☑ 模板B  ☐ 模板C  │
│  └─ ...                       │
│                                │
│         [取消]     [确认发起]   │
└────────────────────────────────┘
```

**交互细节**：
- 选择模板后自动加载模板节点并展开节点配置区域
- 节点配置中未填的字段使用模板默认值
- 发起前检查实例名称唯一性（实时异步校验）
- 确认发起后跳转到实例详情页

### 4.6 实例详情 `/flows/instances/{id}`

```
┌──────────────────────────────────────────────┐
│ ← 返回   项目名称 [优先级Badge] [难度Badge]    │
│           状态标签 · 发起人 · 发起时间         │
├──────────────────────────────────────────────┤
│  实例信息卡片（名称/组织/状态/优先级/难度/...） │
│  [终止流程] [修改优先级] (发起人可见)           │
├──────────────────────────────────────────────┤
│  节点卡片列表（按 sort_order 排列）            │
│  ┌──────────────────────────────────────┐    │
│  │ ● 节点1 (开始) — 已完成              │    │
│  ├──────────────────────────────────────┤    │
│  │ ◉ 节点2 (工作) — 运行中              │    │
│  │   文件区 │ 校验区 │ 审批区 │ 批准区    │    │
│  │   [紧急换人] (发起人)                 │    │
│  ├──────────────────────────────────────┤    │
│  │ ○ 节点3 (结束) — 等待中              │    │
│  └──────────────────────────────────────┘    │
├──────────────────────────────────────────────┤
│  操作日志时间线                               │
│  2026-07-24 10:30  张三 发起了项目            │
│  2026-07-24 10:35  李四 提交了节点「XX」      │
│  ...                                        │
└──────────────────────────────────────────────┘
```

**节点卡片（NodeCard）**：四列布局
- **文件列**：列出节点文件，可点击预览/下载
- **校验列**：列出校验人及状态（pending/passed/returned）
- **审批列**：列出审批人及状态（pending/approved/rejected），含签名标记
- **批准列**：列出批准人及状态（pending/approved/rejected），仅 difficulty=4 显示

**操作按钮（实例级）**：
- **终止流程**（发起人，任意状态）：弹出原因输入框 → 确认 → 文件物理删除
- **修改优先级**（发起人，仅 running）：下拉选择新优先级
- **紧急换人**（发起人，节点级）：对未完成节点弹出换人对话框
- **补交文件**（负责人/发起人）：上传补充文件（upload_type=supplement）
- **永久删除**（管理员，仅 terminated）：物理删除实例及文件

**操作按钮（节点级，在 NodeCard 内部）**：
- 待处理任务 → 点击进入 TaskDetail 处理页
- 待校验 → 点击进入 CheckDetail 处理页
- 待审批 → 点击进入 ApprovalDetail 处理页
- 待批准 → 点击进入 EndorseDetail 处理页

### 4.7 模板详情 `/flows/detail/{templateId}`

```
┌──────────────────────────────────────────────┐
│ ← 返回  模板名称  状态标签                     │
├──────────────────────────────────────────────┤
│  模板信息：名称、描述、类型、所属组织、节点数    │
│  [编辑] [设计器] (所长可见)                    │
├──────────────────────────────────────────────┤
│  Tab: [节点列表] [文件模板]                    │
│                                              │
│  节点列表 Tab:                                │
│  ┌──────────────────────────────────────┐    │
│  │ # │ 名称 │ 类型 │ 负责人 │ 校验人 │ 审批人│ │
│  │ 1 │ 开始 │ 开始 │   -    │   -    │   -  │ │
│  │ 2 │ 编写 │ 工作 │  张三  │  李四  │  王五│ │
│  │ 3 │ 结束 │ 结束 │   -    │   -    │   -  │ │
│  └──────────────────────────────────────┘    │
│                                              │
│  文件模板 Tab:                                │
│  ┌──────────────────────────────────────┐    │
│  │ 模板名称 │ 文件类型 │ 文件大小 │ 操作   │    │
│  │ 技术模板 │  .docx   │  50KB   │ [下载]│    │
│  └──────────────────────────────────────┘    │
└──────────────────────────────────────────────┘
```

**交互细节**：
- 模板详情页为只读展示，编辑跳转到设计器
- 节点列表显示模板节点的完整配置（非实例快照）
- 文件模板 Tab 显示已关联的文件模板，可在此关联/取消关联
- 所长可见编辑和设计器入口按钮

### 4.8 边界情况

- **实例名称重复**：发起时实时校验，已存在则提示"实例名称已存在"
- **模板已删除**：实例仍可正常查看（template_name 快照），但面包屑中模板链接失效
- **节点无文件**：文件列显示"暂无文件"
- **节点无校验人**：提交后直接进入审批（跳过校验）
- **节点无审批人**：提交/校验通过后报错（发布校验确保不会出现）

---

## 5. 方案管理

方案是第二流程类型（`template_type="proposal"`），与项目共享全部后端模型和前端组件。

### 5.1 与项目的差异

| 页面 | 差异 |
|------|------|
| 组织卡片页 | 使用方案统计数据（方案进行中数/总数/本月数） |
| 全部方案 | 表格列较少，无进度条/难度，可点击查看详情 |
| 方案详情 | 复用 InstanceDetail 组件，面包屑/标题区分"方案详情" |
| 完成逻辑 | **工作节点审批通过后直接完成**（跳过结束节点终审） |

### 5.2 方案详情页

复用 `InstanceDetail.vue`，通过路由参数区分：
- `/flows/instances/{id}` → 显示"项目详情"
- `/proposals/instances/{id}` → 显示"方案详情"
- 面包屑分别链接到开工项目 / 方案管理

### 5.3 方案关联

- 项目可关联一个已完成的方案（`proposal_id`）
- 发起项目时可选择已完成的方案
- 关联后可在项目详情中查看

---

## 6. 个人中心

### 6.1 访问权限

- 仅所长（manager）和普通用户（user）可见
- 系统管理员不显示此菜单

### 6.2 Tab 结构

**所长（9 Tab）**：

| Tab | API | 默认筛选 |
|-----|-----|----------|
| 我的待办（项目） | GET /tasks?instance_type=project | 排除 completed/terminated |
| 我的校验（项目） | GET /checks?instance_type=project | status=pending |
| 我的审批（项目） | GET /approvals?instance_type=project | status=pending |
| 我的批准（项目） | GET /endorsements?type_filter=project | status=pending |
| 我发起的（项目） | GET /instances/my-initiated?type=project | — |
| 我的待办（方案） | GET /tasks?instance_type=proposal | 排除 completed/terminated |
| 我的审批（方案） | GET /approvals?instance_type=proposal | status=pending |
| 我的批准（方案） | GET /endorsements?type_filter=proposal | status=pending |
| 我发起的（方案） | GET /instances/my-initiated?type=proposal | — |

**普通用户（6 Tab）**：无"我发起的"Tab。

### 6.3 通用交互

- 每个 Tab 支持：关键词搜索（按实例名称）、分页、page_size 选择器（20/50/100）
- 待办列表按**逾期优先**排序（deadline 已过 → deadline 近 → deadline 远）
- 列表项显示：实例名称、节点名称、提交人/发起人、状态、截止日期（逾期红色）
- 点击列表项 → 跳转对应处理页

### 6.4 任务处理页 `/profile/task/{id}`

```
┌──────────────────────────────────────────────┐
│ ← 返回  项目名称  节点名称  优先级  进度条     │
├──────────────────────────────────────────────┤
│  节点信息：描述、负责人、时限、截止日期、状态  │
│  退回信息：(如有) 校验退回 / 审批驳回 原因     │
├──────────────────────────────────────────────┤
│  文件上传区域                                 │
│  ┌─ 文件夹A (必须, 需要2个) [已提交 1/2]     │
│  │  ├ 📄 report.pdf                     删除 │
│  │  └ [选择文件] [上传]                      │
│  ├─ 文件夹B (可选)                           │
│  │  └ [选择文件] [上传]                      │
│  └─ [+ 自由上传]                             │
│  (若无文件夹配置，显示自由上传区域)            │
├──────────────────────────────────────────────┤
│  签批预览 (如有签名图片)                      │
│  ┌─ 📄 report.pdf                          │
│  │  签名位置：[X] [Y] [页码] [预览]          │
│  └─ ...                                    │
├──────────────────────────────────────────────┤
│  负责人备注                          [_____]  │
│         [保存草稿]                   [提交]   │
└──────────────────────────────────────────────┘
```

**交互细节**：
- 打开页面 → Task 状态 pending → processing（自动开始）
- 上传后文件出现在对应文件夹下
- 提交时：非 PDF 自动转换 → 签名写入 PDF（如开启）→ 创建校验/审批记录
- 提交成功后跳转回个人中心

### 6.5 校验处理页 `/profile/check/{id}`

```
┌──────────────────────────────────────────────┐
│ ← 返回  项目名称  节点名称                     │
├──────────────────────────────────────────────┤
│  文件列表（可预览/下载）                       │
│  📄 report.pdf  [预览]                      │
├──────────────────────────────────────────────┤
│  校验进度（显示所有校验人状态）                 │
│  张三 ✅ 已通过  李四 ⏳ 待校验  王五 ❌ 已退回 │
├──────────────────────────────────────────────┤
│  签名配置 (如有签名图片)                       │
│  签名位置：[X] [Y] [页码]                     │
├──────────────────────────────────────────────┤
│  校验意见                          [_______]  │
│              [退回(需填写意见)]      [通过]    │
└──────────────────────────────────────────────┘
```

**交互细节**：
- 退回必须填写意见
- 通过后：非最后一人 → 等待其他人；最后一人 → 全部通过 → 进入审批阶段
- 操作后自动跳转回个人中心

### 6.6 审批处理页 `/profile/approval/{id}`

```
┌──────────────────────────────────────────────┐
│ ← 返回  项目名称  节点名称  进度条             │
├──────────────────────────────────────────────┤
│  校验进度（只读）                             │
│  审批进度（显示所有审批人状态）                 │
├──────────────────────────────────────────────┤
│  文件列表（可预览/下载）                       │
├──────────────────────────────────────────────┤
│  [终审节点] 驳回目标节点选择                   │
│  ○ 节点2 (技术方案)  ○ 节点3 (产品设计)       │
├──────────────────────────────────────────────┤
│  签名配置 (如有签名图片)                       │
│  签名位置：[X] [Y] [页码] [预览签名位置]       │
├──────────────────────────────────────────────┤
│  审批意见                          [_______]  │
│              [驳回(需填写意见)]      [通过]    │
└──────────────────────────────────────────────┘
```

**交互细节**：
- 中间节点驳回：固定回到当前节点负责人，不需要选目标
- 终审节点驳回：必须选择目标中间节点，填写驳回意见
- 难度4流程：审批全部通过后不立即推进，而是进入批准环节
- 操作后自动跳转

### 6.7 批准处理页 `/profile/endorse/{id}`

与审批处理页结构类似，但：
- 只在 difficulty=4 流程中出现
- 批准人只有一人（单人决策）
- 驳回时节点回到 running 状态，负责人重做

---

## 7. 系统管理

### 7.1 访问权限

仅系统管理员可见。一个页面 5 个 Tab。

### 7.2 用户管理 Tab

- 表格：用户名、姓名、组织、角色、邮箱、手机、状态（启用/禁用）
- 搜索：用户名/姓名模糊搜索 + 组织筛选
- 操作：新增（弹窗表单）、编辑、启用/禁用、重置密码

### 7.3 组织管理 Tab

- 表格：组织名称、描述、所长、用户数、状态
- 操作：新增、编辑、启用/停用（不物理删除）

### 7.4 角色管理 Tab

- V1 只读展示三个预置角色（system_admin/manager/user）
- 显示每角色的用户数

### 7.5 系统配置 Tab

- 键值对列表，按类别分组
- 可编辑项：
  - 文件上传大小限制（MB）
  - 允许的文件 MIME 类型（多选）
  - Token 过期时间（分钟）
  - 系统名称
  - 签名默认坐标 X/Y/偏移量/页码/最大宽高
  - 归档目录名称
  - LibreOffice 路径
- 保存后写入 DB + 刷新内存缓存

### 7.6 文件模板 Tab

- 表格：模板名称、文件名、组织、类型（docx/xlsx）、大小、上传时间
- 组织筛选
- 操作：上传（≤10MB）、删除（物理删除文件）

---

## 8. 通知系统

### 8.1 铃铛入口

- 位置：面包屑栏右侧（所有页面除 Dashboard 和设计器）
- Dashboard 页面：header 右侧
- 有未读时红点 + 计数（>99 显示"99+"）

### 8.2 实时推送

- WebSocket 连接到 `/api/v1/ws?token=JWT_TOKEN`
- 新通知 → 右下角弹出气泡（5 秒自动消失）
- 气泡内容：通知标题 + 内容摘要
- 点击气泡 → 跳转目标链接 → 自动标记已读

### 8.3 下拉面板

- 最近 20 条通知列表
- 未读：蓝色背景 + 蓝色圆点
- 每项：类型图标 + 标题 + 时间（如"3分钟前"）
- 点击 → 跳转 + 自动标记已读
- 底部"全部已读"按钮
- 滚动加载更多

### 8.4 通知类型与跳转

| 类型 | 点击跳转 |
|------|----------|
| task_assigned | /profile/task/{id} |
| check_assigned | /profile/check/{id} |
| approval_assigned | /profile/approval/{id} |
| endorsement_assigned | /profile/endorse/{id} |
| check_returned | /profile/task/{id} |
| approval_rejected | /profile/task/{id} |
| final_rejected | /profile/task/{id} |
| endorsement_rejected | /profile/task/{id} |

---

## 9. 个人信息

右上角用户名下拉菜单：

| 菜单项 | 功能 |
|--------|------|
| 个人信息 | 只读弹窗：用户名、姓名、组织、角色、邮箱、手机 |
| 签名图片 | 上传 PNG 签名（透明底，自动去白底+缩放，≤500KB） |
| 修改密码 | 旧密码 + 新密码 + 确认密码，成功后**自动退登** |

---

## 10. 文件模板系统

### 10.1 模板上传

管理员在系统管理 > 文件模板上传 `.docx` 或 `.xlsx` 文件，关联到组织。

### 10.2 模板关联

- 设计器中关联到流程模板（多对多）
- 发起实例时继承模板关联，可调整实例级列表
- 实例级调整存储在 `flow_instances.doc_template_ids` JSON 字段

### 10.3 变量替换

文件模板支持 `{{变量名}}` 占位符。支持的变量：
- `{{项目名称}}` → 实例 name
- `{{合同号}}` → 实例 contract_no
- `{{产品型号}}` → 实例 product_model
- `{{销售经理}}` → 实例 sales_manager

任务负责人下载模板时，系统自动替换占位符为实例实际值。

### 10.4 下载流程

1. 负责人在任务详情页点击"文件模板"按钮
2. 弹窗列出可用的文件模板列表
3. 选择模板 → 系统替换变量 → 返回 `.docx`/`.xlsx` 文件下载

---

## 11. 全局交互规范

### 11.1 加载状态

- 列表/表格：el-skeleton / v-loading
- 详情页：整体 loading，避免局部闪烁
- 按钮操作：loading 状态 + 禁用重复点击

### 11.2 空状态

- 列表无数据：el-empty（"暂无数据"）
- 搜索无结果：el-empty（"未找到匹配结果"）

### 11.3 错误处理

- 网络错误：统一 message.error（"网络请求失败，请稍后重试"）
- 业务错误：展示后端返回的 message
- 403：跳转 /403 页面
- 401：清除 token → 跳转登录页

### 11.4 确认操作

- 删除、终止、驳回、退回等不可逆操作 → el-popconfirm 二次确认
- 终止流程 → 弹出对话框输入原因

### 11.5 分页统一

所有分页列表使用统一组件，支持 page_size 选择器（20/50/100），默认 20。

---

## 12. 边界异常场景

### 12.1 用户状态变更

| 场景 | 系统行为 |
|------|----------|
| 用户被禁用 | 已持有的 Token 仍然有效直到过期；新登录被拒绝（40300）；已被分配的待处理任务/校验/审批仍存在，但该用户无法登录操作。**建议管理员同步换人** |
| 用户被删除 | 数据库中保留其操作记录（operation_logs.operator_id 保留原值）；历史审批/校验记录保留其姓名快照；已被分配但未处理的 pending 记录需要管理员手动换人 |
| 用户被移出组织 | 不影响已分配的跨组织任务；个人中心仍可看到自己的待办 |

### 12.2 流程异常

| 场景 | 系统行为 |
|------|----------|
| 模板被删除 | 已运行的实例不受影响（template_name 快照），但实例详情中模板链接失效；无法基于此模板发起新实例 |
| LibreOffice 转换失败 | 任务提交失败，返回 50001 "文件转换失败，请检查文件格式后重试"；Task 状态不变，文件保留 |
| 转换超时 | asyncio.Semaphore(4) 限流（worker 端 max_jobs=4） + 单文件转换无超时限制；极端情况下进程卡死需运维介入 kill soffice 进程 |
| 并发校验/审批 | FOR UPDATE 行锁保证同一节点的并发操作串行化；后到达的事务等待前一个释放锁 |
| 驳回时物理文件删除失败 | 仅记录 warning 日志，不影响 DB 状态和流程推进 |

### 12.3 通知与 WebSocket

| 场景 | 系统行为 |
|------|----------|
| WebSocket 断连 | 前端自动重连（指数退避）；断连期间的通知通过下次拉取 `GET /notifications` 获取 |
| WebSocket 推送失败 | 通知已写入 DB，仅推送失败；用户刷新页面或下拉面板时可看到 |
| 通知创建失败 | try/except 捕获，记录 error 日志，不影响主业务流程 |
| 未读计数超过 99 | 铃铛图标显示 "99+" |

### 12.4 文件操作

| 场景 | 系统行为 |
|------|----------|
| 上传文件超过 50MB | 后端返回 413/422，前端在文件选择时也做 size 校验（Element Plus before-upload） |
| 上传不被允许的文件类型（如 .exe） | 后端返回 41500 "不支持的文件类型"，前端也做扩展名校验 |
| 磁盘空间不足 | 文件写入失败 → 异常抛出 → 事务回滚 → 返回 500 错误；不会出现 DB 有记录但文件不存在的情况（先写文件后写 DB） |
| 提交时无文件但 require_file=true | 后端校验返回 40000 "该节点要求必须上传文件" |
| 文件夹配置校验失败 | 后端返回 40000 "文件夹「XX」必须至少提交 1 个文件" 或 "文件夹「XX」需要提交 2 个文件，当前已提交 1 个" |

### 12.5 限流

| 场景 | 系统行为 |
|------|----------|
| 触发限流 | 返回 429 `{"code":42900, "message":"请求过于频繁，请稍后重试"}` + `Retry-After: 60` header |
| 管理员操作 | 不触发限流（每个管理员请求带唯一 uuid 标记） |
| 多用户共享 IP（如 NAT） | 登录接口按 IP 限流（20/min），共享 IP 环境下可能误触发；其余接口按 JWT user_id 限流无此问题 |

---

## 13. 验收标准

- ✅ 完整流程闭环：设计 → 发布 → 发起 → 执行 → 校验 → 审批 → [批准] → 终审 → 归档
- ✅ fork/join 并行分叉汇合正常（任意深度嵌套）
- ✅ 驳回：校验退回、中间审批驳回、终审总驳回、批准驳回 — 四种路径全部覆盖
- ✅ 难度 4 流程批准人环节完整
- ✅ 紧急换人不影响已完成记录
- ✅ 终止流程：文件物理删除 + 所有 pending 记录关闭
- ✅ WebSocket 实时通知（9 种类型 + 气泡弹出）
- ✅ 方案与项目双类型（共享模型，独立路由，差异处理）
- ✅ 文件模板变量替换 + 多对多关联
- ✅ API 限流（三层分级）
- ✅ 补交文件不影响流程状态
- ✅ 所有分页列表支持 page_size 选择器
- ✅ 所有列表支持关键词搜索 + 多条件筛选
