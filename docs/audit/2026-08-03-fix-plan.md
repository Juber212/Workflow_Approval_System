# 修复计划 — 2026-08-03 全栈审查

> 关联报告：`2026-08-03-code-review.md`。分 P0（上线阻断）/ P1（尽快）/ P2（重构清理）三档，建议按序执行，每档完成即回归验证。

> **内网部署校准（2026-08-03）**：产品确认「实例详情 / 全系统待办/超期」全员可见 → 原「实例详情无鉴权」**不修**（报告标注为产品确认项）；模板下载改标为「接口补归属校验」以对齐产品预期（仅任务参与者可下载）。越权「写/篡改/伪造」类（folder_name 穿越、签名 file_id）与正确性类（卡死/死循环/build）不变，仍为 P0。

## 修复进度（2026-08-03）
| 项 | 状态 |
|----|------|
| P0-1 模板 ZIP 下载接口补归属校验 | ✅ 已修复（含单元测试） |
| P0-2 folder_name 路径穿越 | ✅ 已修复（含单元测试） |
| P0-3 签名 file_id 越权 | ✅ 已修复（含单元测试） |
| P0-4 审批 ABBA 死锁 | ❌ **已撤销**（用户确认前端已单选、无多人审批场景，死锁不可达；代码与测试已回滚） |
| P0-5 fork/join 驳回卡死 | ✅ 已修复（helper 单测覆盖；端到端 fork/join 驳回场景待后续补 MySQL 测试） |
| P0-6 must_change_password 死循环 | ✅ 已修复（守卫停留登录页 + 被重置后先登录再改密流程修正 + 后端强制改密免旧密码） |
| P0-7 前端 57 个 TS 错误 | ✅ 已修复（vue-tsc -b 0 errors + npm run build 通过，57→0） |
| P0-8 WS 鉴权完整化 | ✅ 已修复（黑名单/is_active 校验 + 登出/禁用断开 WS + WS 豁免限流） |
| P0-9 角色降级即时生效 | ✅ 已修复（get_current_active_user 重查 DB 实时角色，require_* 基于最新角色） |
| **P0 全部** | ✅ **P0-1/2/3/5/6/7/8/9 完成（P0-4 已撤销）** |
| P1-1 设计器节点 CRUD 归属校验 | ✅ 已修复（含跨模板越权测试） |
| P1-2 任务维度模板下载归属校验 | ✅ 已修复（含跨实例越权测试） |
| P1-3 overdue 数据最小化 | ✅ 产品确认维持全员可见，文档明示（不修） |
| P1-4 模板文件列表组织隔离 | ✅ 已修复（含普通用户/跨组织 403 测试） |
| P1-5 改密/重置密码吊销既有 token | ✅ 已修复（密码版本号 + 改密重签发 token，含集成测试） |
| P1-6 签名图片接口收敛 | ✅ 已修复（仅本人可访问，比「流程参与者」更严格——四个详情接口均仅操作人本人可看，含集成测试） |
| P1-7 通知清理增加 instance_id 维度 | ✅ 已修复（Notification 加 instance_id 列 + 迁移；17 创建点 + 14 清理点按实例 + 历史 NULL 通知兼容一并清，含集成测试 4 条） |
| P1-8 换负责人覆盖 WAITING_* 状态 | ✅ 已修复（活跃任务判定改 notin_ 终结状态；校验/审批/批准人变更 task_id 关联活跃任务；负责人已提交后禁止换负责人；changes 用人名；换人向被换人员实时推送；校验/审批人完成后禁换） |
| P1-9 补交文件文件夹校验 | ✅ 已修复（白名单 + 必填 + 补交文件转 PDF 支持在线预览，含 6 条单测） |
| P1-10 难度4 强制批准人校验 | ✅ 已修复（实例发起时校验难度4工作节点已配批准人，含单测） |
| P1-11 all_approve 聚合与 signature_applied 按 task_id 限定 | ✅ 已修复（聚合查询与签名状态批量更新均补 `Approval.task_id == a.task_id`，与 single_approve 对齐，防跨轮次残留 PENDING 误计数/误标签名，含单测 2 条） |
| P1-12 被终止人员待办通知清理 | ✅ 已修复（新增 `clear_related_for_users` helper，6 处「批量置 TERMINATED」前先收集被终止人员再清对应待办通知：single_approve/终审驳回/驳回到历史/退回负责人/校验退回/批准驳回，含单测 1 条） |
| P1-13 endorse 签名 file_id 与 signature_applied | ✅ 已修复（旧版单签名兼容分支补 file_id=当前轮次首 PDF，无 PDF 不建签名；`signature_applied` 改按待落盘签名结果标记，含单测 2 条） |
| 用户实测 bug：签批配置不生效 | ✅ 已修复（2026-08-03 P1-13 后实测）——`require_endorser_signature`/`endorser_id` 保存与加载链路 3 断点：① designer_service `_NODE_UPDATABLE_FIELDS` 白名单缺字段→保存被丢弃（DB 恒默认）；② template_service `_node_to_dict` 序列化缺字段→前端读不到；③ FlowDesigner 加载时节点 properties 未映射两字段→`?? true` 恢复默认。三处补齐 + 前端类型补字段，含单测 2 条 |
| 用户实测 bug：批准人显示ID + 详情缺难度 | ✅ 已修复（2026-08-03 签批链路修复后实测）——① 设计器批准人显示"用户3"：`get_template_detail` 收集 user_ids 漏 `endorser_id` + `_node_to_dict` 未返回 `endorser_name`，补齐后加载映射 `endorser_name`；② 项目详情页信息网格无难度：InstanceInfo 补「难度」项（复用列表页 `diff-badge` 样式） |
| 用户反馈：详情页信息网格 3 行 4 栏不齐 | ✅ 已修复（2026-08-03）——难度 badge 从信息网格移到标题区与优先级并列（难度属流程等级属性，并列更自然），信息网格 9 项 → 8 项 = 正好 2 行满格 |
| P1-14 多审批人签名偏移/死代码 | ✅ 已修复（2026-08-03，用户选「只删死代码」）——删除无调用的 `apply_signatures_to_node_pdfs`（150 行，含误导性「多审批人偏移」实现）+ 清理多余 import（Approval/InstanceNode）。单审批人场景无同坐标覆盖问题，活跃路径不动 |
| P1-15 PDF转换重试上限 | ✅ 已修复（`convert_all_files_job` 加 `attempt` 参数 + `_CONVERT_ALL_MAX_ATTEMPTS=20`，约60秒兜底对齐前端轮询；超限把仍 pending/converting 文件标 failed「转换超时」并通知前端，停止无限自重新入队，含单测 3 条） |
| 用户反馈：上传后文件显示与转换时机不符 | ✅ 已修复（2026-08-03 方案A）——正常任务流程转换时机保持「提交时」（符合 CLAUDE.md 设计）；上传的 Word/Excel 现标 `conversion_status=pending`，task 详情序列化补该字段，前端文件行显示「待转换/转换中/转换失败」标签且未转换时隐藏「查看」按钮，杜绝「上传后显示 ready 但实际未转」的误导 |
| P1-16 优先级排序 deadline 恒 NULL | ✅ 已修复——`list.py` 排序子查询原用 `["pending","processing"]`（非实例节点状态，恒匹配不到）；`_helpers.py` 新增 `ACTIVE_NODE_STATUSES` 常量（arrived/running/pending/processing/waiting_*），list 子查询 + 活跃节点查询 + 活跃 deadline 三处复用，「最近截止优先」恢复生效，含单测 1 条 |
| P1-16 补：优先级排序顺序反了 | ✅ 已修复（2026-08-03 实测反馈）——原 `order_by` 把 deadline 排在 priority 前，导致截止时间早的普通项目压过紧急项目（P1-16 前 deadline 恒 NULL 掩盖了该顺序错误）；修正为 priority 主导 → 同优先级内 deadline 最近优先 → 无 deadline 最后，测试补 order_by 顺序断言 |
| P1-17 deadline 逾期口径 | ✅ 已修复（2026-08-03，用户选「不改存储」）——deadline 存当日 00:00:00，旧 `deadline-now` 口径在截止日当天 00:01 起误报「已逾期1天」。判断层统一自然日：`_helpers.py` `compute_deadline_info` 改自然日 + 新增 `is_deadline_overdue`，`task_service`/`notification_service`(4处)/`dashboard_service` 超期列表全部复用，杜绝「通知说逾期、列表说未逾期」矛盾；存储值不改。新增单测 10 条（281 passed） |
| P1-18 无负责人节点换人后激活 | ✅ 已修复（2026-08-03）——waiting 节点因发起时未配负责人被 propagate「无负责人守卫」拒绝，卡死无法激活；换人只更新 assignee_id 不触发激活。抽 `activate_work_node`（flow_engine）供 propagate 与 change 复用；`change_personnel` 换负责人后若 waiting && arrived_count>=incoming_count 立即激活生成 Task。实测发现补丁：waiting 节点三字段一起改（负责人+校验人+审批人）时无 Task 导致 CheckRecord/Approval task_id=NULL 报 500 → 预激活前置（角色分支前先生成 Task）+ 无 Task 时不建角色记录（等激活后由提交流程按配置生成），新增单测 2 条（283 passed） |
| P1-19 换人/审批 TOCTOU | ✅ 已修复（2026-08-03）——approve/reject 读 node 用普通 SELECT，与 change_personnel 的 node 行锁互不阻塞，并发时基于旧状态写入（如换人改人员 vs 审批推进节点）。approve 与 reject 的 node 查询均改 `FOR UPDATE`（approval_service），与换人锁同一 node 行串行化。新增单测 2 条（285 passed） |
| P1-20 模型索引对账 | ✅ 已修复（2026-08-04）——DB 已有大量索引但模型全未声明（metadata 与 DB 脱节，autogenerate 必产生大量误删）。13 表模型补 `__table_args__` 索引声明，索引名与 DB 完全对齐，并补 DB 缺失热点索引：`flow_instances.idx_status/idx_template_type`、`notifications.idx_instance`、`signatures.idx_source`；User 补 `idx_org`；UserRole 补 FK 声明（DB 初始已有 CASCADE FK，仅元数据对齐，不重复建）。Alembic 迁移 `a7b8c9d0e1f2` 只增不删，已应用开发库并验证。pytest 285 + MySQL 20 passed。约定：此后禁用 autogenerate 直改生产 |
| P1-21 create 响应补 difficulty | ✅ 已修复（2026-08-04）——`create_instance` 返回的 `InstanceResponse` 构造时漏传 `difficulty`（schema 字段早已声明、前端 `InstanceCreated` 类型也有），发起后难度恒回显默认「1」。补 `difficulty=instance.difficulty`，发起日志 detail 一并记录难度。pytest 265 + MySQL 20 passed |
| P1-22 人员 ID 存在性校验 | ✅ 已修复（2026-08-04）——创建实例 `node_overrides` 与保存模板节点的人员 ID 均未校验存在性，悬空引用会把任务分派给不存在的用户。`validation_service.py` 新增 `extract_person_ids`（兼容 dict/int 两种格式）+ `validate_user_ids_exist`（批量 IN 查询返回缺失集合）；`create.py` 在 override_map 构建后校验，`designer_service.save_design_data` 在写操作前校验并指明节点名。新增单测 2 条（267 + MySQL 20 passed） |
| P1-23 bcrypt 72 字节截断 | ✅ 已修复（2026-08-04）——bcrypt 只处理前 72 字节，超长密码被静默截断：`"a×100"` 与 `"a×72+任意后缀"` 可过同一密码。**实现走了 service 层而非 schema**：`security.py` 新增 `ensure_password_byte_limit`（按 UTF-8 字节校验，中文 24 字符=72 字节，纯字符限制拦不住）并接入 `validate_password_strength`（改密）；`login`/`change_password` handler 入口分别校验登录密码与旧密码。不用 pydantic `max_length` 的原因：约束错误是英文 422 且前端取 `message` 字段显示，体验差；AppException 中文错误（400）+ `message` 字段前端直接显示。管理员重置/创建用户走默认密码，无请求体，不受影响。新增测试 4 条（271 + MySQL 20 passed） |
| P1-24 缺失 Authorization 头 | ✅ 已修复（2026-08-04）——`deps.py:38` `Header(...)` 必填参数，缺失 Authorization 头时 FastAPI 在依赖解析阶段判 **422**（`String should have at least 1 characters` 英文），前端 401 跳转逻辑只认 401 → 未登录访问 API 显示「请求失败」而非跳登录。改 `Header(None)` 可选 + 函数内 `not authorization` 手动抛 `UNAUTHORIZED`（401），前端正确跳转登录并保留页面。新增测试 1 条（272 + MySQL 20 passed） |
| P1-25 限流待办 | ✅ 已修复（2026-08-04）——① **不信任 XFF**：`_get_limit_key` 未认证分支原取 `X-Forwarded-For`（可被客户端任意伪造，变换伪造 IP 即可绕过限流），改一律用 `request.client.host`（内网直连即真实来源；若未来加反代需 trusted proxy 配置而非信请求头）。② **`_SlidingWindow` 内存防护**：加 `_MAX_KEYS=10000` 上限（超限清过期→淘汰最早 key）+ `_CLEAN_EVERY_CALLS=500` 定期全量清理。新增测试 2 条 + 改 XFF 用例（274 + MySQL 20 passed）。实测：curl 350 次 → 246 次 401 + 104 次 429，限流生效；发现 localhost 在 Windows 轮换 IPv4/IPv6 导致 key 分叉（需用 127.0.0.1 直连测） |
| P1-26 Redis 黑名单超时 | ✅ 已修复（2026-08-04）——`get_token_blacklist_redis` 原无 socket 超时：黑名单中间件每请求查 Redis，Redis 挂起时默认连接阻塞数秒~数十秒拖垮全站。加 `socket_connect_timeout=1, socket_timeout=1`，1 秒快速失败后走 `is_blacklisted` 的 except 分支放行。新增测试 2 条（超时参数 + 惰性单例，276 + MySQL 20 passed） |
| P1-27 DB 连接串编码 | ✅ 已修复（2026-08-04）——`config.database_url` 原样拼接 `DB_USER:DB_PASSWORD`，密码含 `@ : / %` 等特殊字符时 SQLAlchemy 误解析连接串（如 `pa@ss` 被当作用户:密码分隔）。改 `urllib.parse.quote_plus(..., safe="")` 编码 user/password；普通账号密码（无特殊字符）连接串不变，现有环境不受影响。新增测试 3 条（含 make_url 解析还原，279 + MySQL 20 passed） |
| P1-28 中间件顺序 + must_change 合并 | ✅ 已修复（2026-08-04）——① **中间件顺序**：Starlette `add_middleware` 插入栈顶，原注册顺序 CORS→RateLimit→Blacklist→MustChange 实际执行 MustChange 最外层、RateLimit 未到最外层。调整后 **RateLimit 最外层**（恶意高频请求先被内存层拦截，不进入 Redis/DB 查询），顺序：RateLimit→Blacklist→CORS→路由。② **must_change 检查合并进依赖**：`MustChangePasswordMiddleware` 每请求独立开 DB 会话查 must_change_password，与 `get_current_active_user` 已查的同一用户重复；删除中间件，在依赖里用同一次查询的 `user.must_change_password` + 注入 Request 判断路径白名单（login/logout/password/me 放行）。新增测试 3 条（must_change 拦截/白名单放行 + 中间件顺序，282 + MySQL 20 passed） |
| P1-29 连接池与 worker 匹配 | ✅ 已修复（2026-08-04）——部署文档启动命令 `--workers 4`，每 worker 独立连接池，总量 = workers×(pool_size+max_overflow)，4×40=160 易超 MySQL 默认 151 触发 "Too many connections"。`config.py` 加 `DB_POOL_SIZE`/`DB_MAX_OVERFLOW`（默认 20/20 可调），`database.py` 改用它；`04_Deployment.md` 启动命令加约束说明（worker×(pool+overflow) ≤ max_connections，4 worker 时建议各 10/5）+ 性能参数表更正。新增测试 1 条（283 + MySQL 20 passed） |
| P1-30 WS 孤儿连接 | ✅ 已修复（2026-08-04 前端）——`notification.ts` 的 `disconnect()` 调用 `ws.close()` 触发 `onclose` → 非 4001 则 `setTimeout(connect, 5000)`，组件已卸载后连接仍被重建（孤儿连接泄漏）。加 `disposed` 标记（`connect()` 入口检查，卸载后所有重连路径拒绝建连）+ `manualClose` 标记（`onclose` 手动关闭不重连）+ `ws.onclose=null`（双保险断开回调）。vue-tsc 0 errors + build 通过。**实测验证**（临时加实例号日志 + 硬刷新）：进入首页 1 个 WS → 切走 disconnect → **等 10 秒无新连接**，孤儿重建确认被拦截。验证后已移除调试日志。注：首页多挂 1 个 NotificationBell（AppLayout + Dashboard 各一），进首页多 1 个 WS 是正常设计，非孤儿 |

---

## P0 — 上线阻断（不修不可上线）

| # | 标题 | 文件 | 修复方案 | 验证 |
|---|------|------|----------|------|
| P0-1 | 模板 ZIP 下载接口补归属校验 | `api/templates.py:463`、`category_service.py` | UI 入口已限任务参与者，但后端接口零校验（构造 URL 即可绕过）。下载前校验 doc_ids 全部属于 `instance.template_id`/`instance.doc_template_ids` 关联集，且用户是该实例参与者或管理员 | 越权测试：非参与者 doc_ids → 403 |
| P0-2 | folder_name 路径穿越 | `file_service.py:102`、`api/tasks.py:221` | ① `folder_name` 必须 ∈ `node.file_folders` 配置的 name（白名单），且不含 `/ \ ..`；② 写盘前 `os.path.realpath` + `commonpath` 断言在 `STORAGE_ROOT/archive_subdir/实例名/` 内 | 恶意 folder_name 测试：`../../..` → 400 |
| P0-3 | 签名 file_id 越权 | `task_service.py:420`、`check_service.py`、`approval_service.py`、`endorsement_service.py` | `create_signature_records` 入口校验：file_id 必须属于本 task（`File.task_id == source_id`）或本 node 且 round 匹配，否则 400 | 越权测试：异实例 file_id → 400 |
| P0-4 | 审批 ABBA 死锁 | `approval_service.py:388`、`check_service.py:267` | 统一锁序：先 `FOR UPDATE` 锁 `InstanceNode` 行（一次串行化整节点），再处理审批/校验；或对节点全部 PENDING 行按 `ORDER BY id` 一次性锁 | 并发测试：双审批人同时 approve → 无 500 |
| P0-5 | fork/join 驳回卡死 | `approval_service.py:681,823`、`flow_engine.py` | 驳回重置时：对汇合节点，将「已 FINISHED 且将重新汇入该汇合点的兄弟分支」一并重置为 WAITING + arrived_count=0；或在 `propagate_from_node` 汇合判断加幂等（上游已 FINISHED 视为已到达）。补悬挂检测日志 | fork/join 模板驳回分支内节点 → 重跑后正常汇合 |
| P0-6 | must_change_password 死循环 | `router/guards.ts:45,73`、`views/login/index.vue`、`api/auth.ts`、`api/auth.py`、`schemas/auth.py` | ① 守卫第 2 步「已登录访问 /login」：先恢复 userInfo，强制改密用户停在登录页（不跳 dashboard），消除 /login↔/dashboard 无限往返；② 登录页去掉自动弹改密窗——被重置用户先输入重置密码登录、登录成功后（must_change_password=true）再弹改密窗；③ 后端改密接口 must_change_password=True 时允许省略 old_password（管理员重置后用户不知旧密码）；④ 改密提交不依赖登录表单密码 | 手动：must_change_password 用户刷新 /dashboard → 停在登录页表单 → 输入重置密码登录 → 弹改密窗 → 改密后进入系统 |
| P0-7 | 前端 57 个 TS 错误 | `types/user.ts`、`api/auth.ts`、`api/designer.ts`、`api/task.ts`、`api/template.ts`、`AppLayout.vue`、`ProgressBar.vue`、`FlowDesigner.vue`、`FlowCanvas.vue` 及 20 余组件 | ① `UserInfo` 增 `has_signature`，`toUserInfo` 映射，`AppLayout` 改用；② 发起人筛选 `initiatorOptions` 改 `UserSearchItem[]`、option value 用 `id`（修复 `u.user_id` 恒为 undefined 的隐藏 bug），ProposalManagement 修正 `searchUsers` 参数；③ `ProgressBar` props 改用 `FlowNodeBrief`（对齐 4 详情页实际返回），`sort_order` 改可选；④ `TemplateEdgeItem` 补 `points`、`DesignerNode.id` 允许临时字符串 ID；⑤ el-select 回调参数放宽兼容 Element Plus 签名；⑥ 清理 16 处未使用变量 + 修复重复 `computed` 导入 | `npx vue-tsc -b` → 0 errors；`npm run build` 通过 |
| P0-8 | WS 鉴权不完整 | `api/ws.py`、`services/ws_manager.py`、`api/auth.py`、`api/users.py`、`core/rate_limit.py` | ① ws.py 认证时查 `is_blacklisted`（登出/禁用后 token 即时失效）+ 查 `User.is_active`（禁用用户不可连），拒绝均 4001；② ws_manager 新增 `disconnect_user`，登出/禁用时断开该用户全部 WS 连接；③ RateLimit 对 WS 升级请求豁免（连接一次低频、认证自负责） | 手动：登出后 WS 断开；禁用用户连 WS → 4001；pytest 221 通过 |
| P0-9 | 角色降级不生效 | `api/deps.py` | `get_current_active_user` 每次请求重查 DB 实时角色（join UserRole/Role），`require_admin/require_manager` 基于最新角色；仅当 DB 角色非空才覆盖 JWT 快照（兼容无角色用户与测试 mock）；`/auth/me` 角色同源，前端刷新同步 | 测试：降级 manager 后旧 token 调管理端点 → 403；pytest 227 通过 |

---

## P1 — 尽快（并发/一致性/越权收敛/测试补真）

### 后端越权与数据暴露
- **P1-1** 设计器单节点 CRUD 归属校验：`designer_service.py` 的 `update_node`/`delete_node` 查询条件加入 `TemplateNode.template_id == template_id`（传参）
- **P1-2** 任务维度模板下载归属校验：`api/tasks.py:401-505` 限定 doc/category 属于 `instance.template_id` 关联集
- **P1-3** overdue/dashboard 数据最小化：✅ 产品确认维持全员可见（2026-08-03），CLAUDE.md 规则 19 明示，不修代码。原建议（仅 admin/manager 全系统）不再适用
- **P1-4** 模板文件列表接口组织隔离：✅ 已修复（`list_document_templates` 加 require_manager + require_same_org，含普通用户/跨组织 403 测试）
- **P1-5** 改密/重置密码吊销既有 token：✅ 已修复。采用**密码版本号**方案（非枚举 jti）——Redis `pwd_v:<user_id>` 存最近一次改密/重置时间戳，`TokenBlacklistMiddleware` 对比 `token.iat`，早于版本号一律 401。改密接口成功后**重新签发 token 返回**，当前会话无缝续期，其他设备旧 token 全部失效。强制改密场景前端 `setToken` 替换会话，不中断
- **P1-6** 签名图片接口收敛：✅ 已修复。经核实四个详情接口（任务/校验/审批/批准）均「仅操作人本人可查看」，`current_signature_url` 指向的正是查看者自己的签名素材，故接口改为**仅本人可访问**（比「流程参与者」更严格）。管理员不参与业务、无查看签名场景，同样不放行。前端所有展示场景（详情页签批预览/个人中心/头像区）均不受影响

### 流程状态与数据一致性
- **P1-7** `clear_related` 增加 `instance_id` 维度：✅ 已修复。Notification 表新增 `instance_id` 列（迁移 `c3d4e5f6a7b8`，可空兼容历史数据）；`create_notification` 增加可选 `instance_id`，17 处创建通知调用点全部传入；`clear_related` 增加可选 `instance_id`，14 处清理调用点全部按实例过滤（终止/驳回/换人/操作完成均只清当前实例通知，不再误清其他实例）
- **P1-8** 换负责人覆盖 `WAITING_*`：✅ 已修复。新增 `_get_active_task` helper，活跃任务判定改为 `status.notin_(["completed","terminated","rejected"])`；校验人/审批人/批准人变更创建新记录时 `task_id` 关联活跃任务（修复 task_id=None 坏记录）；新负责人通知覆盖活跃任务。**用户实测修正（三次）**：① 负责人已提交（waiting_*）后禁止更换负责人；② changes 文案用人名而非裸 ID（批量查 User 映射）；③ 换人后向被换掉的人员推送 refresh_count 实时刷新角标；④ 校验人/审批人对应阶段完成后也禁止更换（负责人→waiting_*，校验人→waiting_approval/endorsement，审批人→waiting_endorsement），前端同态禁用字段
- **P1-9** 补交文件校验：✅ 已修复（用户选「白名单 + 必填」，精确数量不强制）。`supplement.py` 补交前校验：节点有文件夹配置时 `folder_name` 必填且属于白名单、必填文件夹（连同历史文件）非空；节点无文件夹配置时 `folder_name` 必须为空。**用户反馈补充**：补交的 Word/Excel 原为下载查看（未转 PDF），现已接入转换队列——非 PDF 补交文件写入后入队转 PDF，`NodeFileBrief` 增 `conversion_status`，前端补交后轮询转换状态、转换完成后在线预览（失败回退下载）
- **P1-10** 难度4 强制批准人：✅ 已修复。模板无 difficulty 字段（难度为实例级），故落点在**实例发起**——`create.py` 难度4 时校验所有工作节点 `endorser_id` 非空，缺失则拒绝发起并列出节点名（否则审批通过后跳过批准环节，难度4形同虚设）
- **P1-11** all_approve 聚合与 `signature_applied` 按 `task_id`/`round` 限定（与 single_approve 对齐）：✅ 已修复。`approval_service.py` 两处补 `Approval.task_id == a.task_id`——① all_approve 聚合查询（检查"是否还有待审批"）只统计当前任务 PENDING，避免跨轮次/跨任务残留 PENDING（如历史 task_id=None 坏数据）误计入导致全通过仍卡等；② `signature_applied` 批量更新只标当前任务 APPROVED 审批，避免多轮次重跑误标历史轮次
- **P1-12** 终止/驳回时对被终止人员的 `check_assigned`/`approval_assigned`/`endorsement_assigned` 通知做 clear_related：✅ 已修复。`notification_service.py` 新增 `clear_related_for_users(db, user_ids, notif_type, instance_id)`；`approval_service.py`（single_approve 终止其他审批、终审驳回、驳回到历史节点、退回负责人）、`check_service.py`（校验退回）、`endorsement_service.py`（批准驳回）共 6 处「批量置 TERMINATED」前先 `select` 被终止记录的归属人员，再清对应待办通知（`terminate.py` 实例终止、`change.py` 换人此前已处理）。先收集后 UPDATE 的原因：UPDATE 后 pending 状态已变，无法再查
- **P1-13** endorse 旧兼容分支 `file_id` 补为当前轮次首 PDF（对齐 approval_service.py:446）；`endorse()` 的 `signature_applied=True` 改为按实际落盘结果：✅ 已修复。旧版单签名兼容分支（`signature_x` 直传）原 `file_id=None`，与模型 `nullable=False` 冲突且签名永不写入 PDF——现对齐 approve 旧版兼容：查节点当前轮次首 PDF 补 `file_id`，无 PDF 则不建签名（杜绝坏记录）；`e.signature_applied` 由恒 True 改为 `bool(_pending_signature_ids)`，仅当存在待落盘签名才标已签名
- **P1-14** 多审批人签名偏移：`apply_signatures_to_files` 中对同文件同坐标签名按 `sort_order` 偏移，或删除误导性死代码 `apply_signatures_to_node_pdfs`：✅ 已修复（用户选「只删死代码」）——单审批人场景无同坐标覆盖问题（同一 PDF 多角色签名靠系统配置角色坐标错开），故不实现偏移；删除无调用点的 `apply_signatures_to_node_pdfs`（150 行）+ 清理仅其使用的 import
- **P1-15** `convert_all_files_job` 加重试上限，超限标记 failed 并通知：✅ 已修复。函数加 `attempt` 参数（默认1），`_CONVERT_ALL_MAX_ATTEMPTS=20`（每次延迟3秒≈60秒兜底，对齐前端补交轮询超时）；未达上限重新入队（attempt+1），达上限把仍 pending/converting 的文件标 `failed`（`conversion_error="转换超时，请重新提交文件"`）并走正常 `partial_failed` 通知，杜绝无限自重新入队消耗资源
- **P1-16** 优先级排序：`list.py:91` 的 deadline 子查询改用活跃状态集合（`running/waiting_*`）：✅ 已修复。原子查询 `status.in_(["pending","processing"])` 匹配不到任何实例节点（节点状态实际为 waiting/running/waiting_*/finished/…）→ `current_deadline` 恒 NULL，「最近截止优先」完全失效。`_helpers.py` 抽 `ACTIVE_NODE_STATUSES` 常量（与既有活跃节点查询一致），`list.py` 排序子查询、`_batch_get_active_node_info`、`_batch_get_active_deadlines` 三处复用，杜绝状态集合漂移
- **P1-17** deadline 语义：`create.py:241` 改设当日 23:59:59.999，或 `compute_deadline_info` 用自然日边界判断：✅ 已修复（用户选「判断层不改存储」）。`_helpers.py` `compute_deadline_info` 改自然日口径（截止日当天不逾期、剩余0天；次日00:00起才算逾期），新增 `is_deadline_overdue` 供直比复用；`task_service` 待办、`notification_service` 超期预警 4 处、`dashboard_service` 超期列表（days_label/is_overdue/排序）统一自然日。注：`notification_service` 用函数内延迟导入避免与 instance 包循环依赖（instance/__init__ → create → flow_engine → 本模块）。新增 `tests/unit/test_deadline_semantics.py` 10 条
- **P1-18** 无负责人节点换人后激活：`change_personnel` 若 `node.status=="waiting"` 且 `arrived_count >= incoming_count` 触发 `propagate_from_node`：✅ 已修复。`flow_engine.py` 抽 `activate_work_node`（waiting→running + 生成 Task + deadline 兜底，通知由调用方负责），`propagate_from_node` 工作节点分支复用（行为等价重构）；`change.py` 负责人变更后若 waiting && arrived_count>=incoming_count 调用之激活（原实现只更新 assignee_id 不触发激活，流程永久死锁）。激活后的负责人通知复用 change 统一通知段（_get_active_task 可查到新 Task），不重复推送。新增单测 1 条（282 passed）。实测补丁（2026-08-04 用户报 500）：waiting 节点三字段一起改时，校验/审批分支 `_get_active_task` 返回 None → CheckRecord/Approval task_id=NULL 违反 NOT NULL。修复：激活逻辑前置到角色分支前（2b 预激活块，先更新负责人+activate_work_node），角色分支仅在 active_task 非空时建记录；移除原 5b 块。新增三字段回归单测 1 条（283 passed），真实 DB 验证通过
- **P1-19** 换人/审批 TOCTOU：`change.py` 与 `approve` 统一先锁 node 行：✅ 已修复。`change.py` 本就锁 node（97 行 with_for_update）；`approval_service.approve`（517 行）与 `reject`（682 行）读 node 原为普通 SELECT，改 `with_for_update()`，与换人锁同一 node 行串行化，消除「换人读旧状态 vs 审批推进节点」竞态。新增单测 2 条（approve/reject 断言 node SELECT 含 FOR UPDATE，285 passed）

### 模型 / Schema / 配置
- **P1-20** 模型索引对账：热点列补 `index=True`、`UserRole` 补 `ForeignKey ondelete=CASCADE`，写「只增不删」对账迁移；此后禁用 `autogenerate` 直改生产：✅ 已修复。模型 `__table_args__` 显式 Index 名对齐 DB（flow_instances/instance_nodes/tasks/check_records/approvals/endorsements/files/notifications/operation_logs/signatures/instance_edges/template_nodes/template_edges/User），补 DB 缺失 4 索引（flow_instances.idx_status+idx_template_type、notifications.idx_instance、signatures.idx_source）；UserRole 补 FK 声明——实测 DB 初始 schema 已有 user_roles_ibfk_1/2（ondelete=CASCADE），迁移不重复建。Alembic `a7b8c9d0e1f2` 只增不删，应用并验证。pytest 285 + MySQL 20 passed
- **P1-21** `create_instance` 响应补 `difficulty=instance.difficulty`：✅ 已修复。`create.py` InstanceResponse 构造补 `difficulty=instance.difficulty`（schema 早有该字段、前端类型也有，仅构造遗漏），发起日志 detail 补 difficulty。无新单测（纯响应补字段），pytest 265 + MySQL 20 passed
- **P1-22** 人员 ID 存在性校验：创建实例/保存模板时对 node_overrides 的 user_id 批量 `IN` 校验：✅ 已修复。`validation_service` 新增 `extract_person_ids` + `validate_user_ids_exist`（批量 IN，返回缺失集合）；`create.py`（override_map 后）与 `designer_service.save_design_data`（写操作前，报错带节点名）两处接入。新增单测 2 条，pytest 267 + MySQL 20 passed
- **P1-23** bcrypt 72 字节截断：改密/登录对超长密码拒绝（max_length）：✅ 已修复。`security.py` 新增 `ensure_password_byte_limit`（UTF-8 字节 ≤72，接入 `validate_password_strength`），`login`/`change_password` handler 入口分别校验登录密码与旧密码。**不走 schema `max_length`**：pydantic 约束是英文 422（实测 `String should have at most 72 characters`），前端取 `message` 字段显示体验差；统一改 AppException 中文错误（HTTP 400）。新增测试 4 条，pytest 271 + MySQL 20 passed
- **P1-24** 缺失 Authorization 头改 401：`deps.py:38` 改 `Header(None)` + 手动抛 UNAUTHORIZED：✅ 已修复。`Header(None)` 可选 + `if not authorization or not authorization.startswith("Bearer ")` 手动抛 UNAUTHORIZED。全站缺头请求由 422 改 401，前端 401 拦截器正确跳登录并保留当前页面。新增测试 1 条（GET /auth/me 无头 → 401），pytest 272 + MySQL 20 passed
- **P1-25** 限流：✅ **全部完成**——`_get_limit_key` 按 `method+path` 独立计数 + 中等阈值 60→300（2026-08-03 用户反馈终止流程 429：各操作共享配额被挤爆）；不信任 XFF（改 `request.client.host`，XFF 可伪造绕过）+ `_SlidingWindow` 加 key 上限（10000）与定期清理（每 500 次调用全量 sweep）
- **P1-26** Redis 黑名单连接加 `socket_connect_timeout=1, socket_timeout=1`：✅ 已修复。`redis.py get_token_blacklist_redis` 加双 1 秒超时，Redis 不可用时黑名单中间件快速失败放行，防挂起拖垮全站。新增单测 2 条（连接参数 + 惰性单例），pytest 276 + MySQL 20 passed
- **P1-27** `config.py:101` 用 `urllib.parse.quote_plus` 编码 DB_USER/DB_PASSWORD：✅ 已修复。`database_url` 对 user/password 做 `quote_plus(safe="")` 编码，特殊字符密码（@/:/%）不再误解析；普通密码不变。新增单测 3 条，pytest 279 + MySQL 20 passed
- **P1-28** 中间件顺序：确认限流在最外层（先注册 MustChange → TokenBlacklist → RateLimit 或调整注册顺序以匹配注释意图）；must_change_password 检查合并进 `get_current_active_user` 同一次查询，消除逐请求多查：✅ 已修复。RateLimit 调整到栈顶（最外层）；删除 MustChange 中间件（含 auth_middleware.py），检查合进依赖（同一次 User 查询 + Request 路径白名单）。新增测试 3 条，pytest 282 + MySQL 20 passed
- **P1-29** 连接池与 worker 数匹配：部署文档约束单 worker 或按 worker 数调 pool_size：✅ 已修复。`config.py` 加 `DB_POOL_SIZE`/`DB_MAX_OVERFLOW`（默认 20/20 可调），`database.py` 改用它；`04_Deployment.md` 启动命令加连接总量约束说明 + 性能参数表更正。新增单测 1 条，pytest 283 + MySQL 20 passed

### 前端
- **P1-30** WS 孤儿连接：`disconnect()` 设 `ws.onclose = null` / `_manualClose` 标记；`connect()` 加 disposed 检查：✅ 已修复（前端）。`notification.ts` 加 `disposed` + `manualClose` 标记，`connect()` 入口检查 disposed，`disconnect()` 双保险（onclose=null + manualClose）。vue-tsc 0 errors + build 通过
- **P1-31** 方案发起人搜索：`searchUsers(query, 20)` + option 用 `u.id`：✅ 已核对（2026-08-04）——三处发起人搜索（FlowManagement/OrgHome/ProposalManagement）均已用 `searchUsers(query, 20)` + `:value="u.id"`（label=real_name、value=id），搜索 API 返回字段正确，无代码改动
- **P1-32** 签名跳转：`profile/index.vue` 响应 `route.query.tab==='signature'` 自动打开上传弹窗，或 AppLayout 响应：✅ 已修复（2026-08-04 前端）。签名上传弹窗在 AppLayout 的个人信息弹窗内（profile 页无签名区），故在 **AppLayout** 响应 `route.query.tab==='signature'`：`watch(route.query.tab)` + `immediate`，命中则 `showUserInfoDialog=true` 并 `router.replace` 清除 query 防刷新重复弹。校验/审批/批准/任务详情页（CheckDetail:264 等）跳转 `Profile?tab=signature` 后即自动弹出上传框。vue-tsc 0 errors + build 通过
- **P1-33** Dashboard 待办总数用 `my_task_counts` 真实计数（或注明「最多显示 8 条」）：✅ 已修复（2026-08-04 前后端）。后端 `_get_my_pending_items` 分组时统计每组真实全量条数（`project_total`/`proposal_total`，不截断），`DashboardData` 新增 `my_pending_total`/`proposal_my_pending_total`；前端"共 N 条"改用真实总数（跟随 Tab 切换项目/方案），真实总数 > 列表条数时旁注「最多显示 8 条」。说明：`my_task_counts` 是项目+方案合计不分 Tab，直接用会导致 Tab=项目 时显示含方案的总数，故后端按 Tab 拆分返回。新增单测 2 条，pytest 305 + MySQL 20 passed；vue-tsc 0 errors + build 通过
- **P1-34** 超期预警页：只展示本人待办，或详情页对 403 专门渲染「无权查看」而非误导空态：✅ 已修复（2026-08-04 前端，方案 2）。超期预警页保持全系统可见（产品确认规则 19，未收窄数据可见性）。`request.ts` 错误 reject 的 Error 附带 `status` 属性（`buildApiError`），4 个详情页（TaskDetail/CheckDetail/ApprovalDetail/EndorseDetail）加载 catch 到 403 时渲染复用 `views/error/403.vue`「无权查看」，取代误导的「记录不存在/任务不存在」空态；load 开头重置 forbidden 防同组件切换路由参数残留。非 403 错误行为不变。vue-tsc 0 errors + build 通过。**补充（用户 2026-08-04 调整）**：超期预警页 4 类「查看」按钮由跳处理页改为跳实例详情页（InstanceDetail，`row.instance_id`），非本人超期项也能查看完整流程上下文（实例详情接口对所有登录用户可见，不再 403 无权查看）
- **P1-35** 错误处理统一：全站 `err?.response?.data?.message` → `err?.message`；视图删除二次弹错（拦截器已弹）；修复 `TaskDetail.vue:539,648` 的「网络连接异常」误报：✅ 已修复（2026-08-04 前端）。根因：拦截器 reject 的 Error 丢弃了原始 `response`（`new Error(msg)`），全站 28 处 `err?.response?.data?.message` 永远取到 undefined，fallback 默认文案导致后端真实原因丢失；且拦截器已 `showErrorOnce` 弹过后端消息，组件再弹是重复 toast。处理：删除 28 处组件 catch 里的 `ElMessage.error(...)` 二次弹错（保留 finally 状态重置，catch 改 `catch { /* 拦截器已统一弹错 */ }`）；TaskDetail 两处 `if (!err?.response) ElMessage.error('网络连接异常')` 删除（因 err?.response 恒 undefined，业务错误 400/403 也误报网络异常）；InstanceDetail 清理未使用 ElMessage import。涉及约 18 个前端文件。登录失败提示保留（已用 err?.message，且拦截器 401 对登录请求不弹）。vue-tsc 0 errors + build 通过
- **P1-36** axios 类型增强：`declare module 'axios'` 重载返回 `Promise<{code;message;data:T}>`，让契约从注释级变编译级
- **P1-37** api 返回值统一为 `return res.data`
- **P1-38** UserSelector：用独立 flag（而非 `options.length`）判断「已加载成员」，修复预填后无法浏览本所成员
- **P1-39** 截止日期语义：前端级联统一调用后端 `calculate-deadlines`，消除 off-by-one 与节假日偏差
- **P1-40** 发起弹窗模板勾选：确认后端 `linked_categories` 必带 documents，否则先拉分类详情
- **P1-41** OrgHome/OrgProposalHome watch `route.params.orgId`
- **P1-42** 个人中心 30s 轮询同时更新 `notifyStore`（WS 断开时侧边栏角标刷新）

### 测试补真（对应报告 5.1/5.2）
- **P1-43** 重写 `test_full_flow.py` 流转测试：直接调真实 service（对齐 `test_service_flows.py`），删除「手工改状态 + 断言自己」
- **P1-44** 补缺失覆盖：fork/join 真实汇合、terminate/reject 物理文件删除、换人成功路径、补交、JWT 黑名单 + must_change_password 中间件、`compute_deadline_info` 超期口径
- **P1-45** 至少一条不 mock `propagate_from_node` 与 `apply_signatures_to_files` 的 MySQL 全链路测试（真实 pypdf 签名）
- **P1-46** 修 3 个「永远通过」测试（`test_edge_cases.py:159`、`test_service_flows.py:394`）；`os.path.exists` 改局部 patch
- **P1-47** 测试库凭据改环境变量（`tests/mysql/conftest.py:17`），从 git 历史清理泄露密码
- **P1-48** 集成测试用 `sqlite_session` 走真实 SQLAlchemy，或删除死 fixture；同步 CLAUDE.md 测试计数与「0 bug」表述

### 部署安全
- **P1-49** 生产守卫：`DEBUG=true` 且非开发环境拒绝启动；SECRET_KEY 最小长度校验 + 拒绝已知默认值
- **P1-50** Swagger 环境开关：`docs_url=None if settings.ENV=="prod" else "/docs"`
- **P1-51** 依赖 pin：`requirements.txt` 精确版本 + 分离 dev 依赖（black/mypy 等）

---

## P2 — 重构 / 清理（质量债，可迭代消化）

| # | 内容 | 说明 |
|---|------|------|
| P2-1 | 删除死代码 | `NodeOverridePanel.vue`（400 行未挂载）、`systemNodeDbIds`、`workNodeCounter`、`apply_signatures_to_node_pdfs`（或修复后接入）、`instance_service.py` shim（二选一） |
| P2-2 | 前端重复代码抽取 | 4 详情页共享「节点信息卡/文件列表/历史文件分组/PDF 列表」组件 + composable；`OrgHome`/`FlowManagement` 抽 `InstanceTable`；`PropertyPanel`/`PresetEditor` 抽 `FolderConfigEditor`；`handleSave`/`handleLaunch` 抽 `buildDesignPayload` |
| P2-3 | 后端四详情接口抽公共 helper | 文件列表 / 用户批量查询 / 节点进度 / 签名默认值 |
| P2-4 | 模型 Schema 对账 | 枚举字符串替换为枚举引用；JSON 字段类型标注统一 `list[dict] | None`；`Signature.role_type` 注释补 endorser；schema 裸 `list[dict]` 收敛为 Brief 模型 |
| P2-5 | 前端一致性 | `deadlineRowClass` 收敛到 `utils/format.ts`；`roleLabel` 文案统一；URL 拼接抽统一 helper；`PriorityEditDialog` watch 刷新；`TerminateDialog` 清空 reason；删 `SignaturePreviewDialog` 调试日志；`getTokenExp` 修 base64url |
| P2-6 | 文档同步 | CLAUDE.md 测试计数（145/28/19）与「0 bug」表述修正、README 并发数（实际 Semaphore 4）、API 文档 category_id 过期注释、`configs.py` 允许 -1（最后一页） |
| P2-7 | 测试环境一致性 | 删除死 fixture（`sqlite_session`/`mock_db_factory`）或让集成测试真正使用；清理 SAWarning 资源泄漏 |

---

## 回归验证清单（每档完成）
1. `python -m pytest tests/ -q` — 全绿，新增越权/并发/补真测试通过
2. `npx vue-tsc -b` — 0 errors；`npm run build` 通过
3. 手动冒烟：实例详情越权 → 403；folder_name 穿越 → 400；must_change_password 刷新无死循环；登出/禁用后 WS 断开；fork/join 驳回重跑正常汇合
